<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <link rel="stylesheet" href="reset.css">
  <link rel="stylesheet" href="style.css">
  <title>Coffeeblog</title>
</head>
<div class="contents">
  <div class="container">
  <h3>投稿フォーム</h3>
    <%= form_with(model: @post, local: true) do |form| %>
      <%= form.text_field :name, placeholder: "Name", class: "text" %>
      <%= form.text_field :image, placeholder: "Image Url", class: "text" %>
      <%= form.text_area :text, placeholder: "text", rows: "10", class: "text" %>
      <%= form.submit "投稿する", class: "text"%>
    <% end %>
  </div>
</div>
